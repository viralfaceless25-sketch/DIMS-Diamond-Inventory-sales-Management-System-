[package]
name = "diamond-inventory"
version = "1.0.4"
description = "Maitri Diamond Inventory desktop client"
authors = ["Maitri Diamonds"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
