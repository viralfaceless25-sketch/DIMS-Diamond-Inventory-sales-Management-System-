# Optional URL of the backend API + Socket.io server.
# Leave blank for LAN/dev: the app will use the same hostname as the browser, port 4000.
# Production example:
# NEXT_PUBLIC_API_URL=https://diamond-inventory-backend.onrender.com
